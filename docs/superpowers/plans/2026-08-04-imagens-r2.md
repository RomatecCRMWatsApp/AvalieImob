# Migração de Imagens (base64-no-Mongo → Cloudflare R2) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Parar de gravar os bytes das imagens/fotos como base64 dentro do MongoDB (`db.images.data_b64`) — que estourou a quota do banco — passando a guardá-los no Cloudflare R2 (já configurado), mantendo 100% de compatibilidade com as imagens já existentes.

**Architecture:** Um módulo central `services/image_store.py` concentra salvar/ler imagem. Toda LEITURA passa por um resolvedor único (`carregar_bytes`/`bytes_do_doc`) que devolve os bytes venham eles do R2 (`r2_key`) ou do base64 legado (`data_b64`) — comportamento idêntico ao atual quando ainda não há R2. Só DEPOIS que todos os leitores estiverem migrados é que a ESCRITA (upload) passa a gravar no R2. Um script opcional migra os `data_b64` já existentes para o R2 e libera o espaço.

**Tech Stack:** FastAPI + Motor (Mongo async) + boto3 (R2, via `services/r2_storage.py`), pytest.

**Ordem de segurança (não inverter):**
1. **Fase 1 — Leitura compatível:** criar `image_store` e trocar TODOS os ~50 pontos que fazem `base64.b64decode(doc["data_b64"])` por `image_store`. Sem R2 configurado o resultado é idêntico (lê `data_b64`). Mergeável sozinha, zero regressão.
2. **Fase 2 — Escrita no R2:** `upload_image` (e páginas de PDF) passam a subir no R2 e gravar só metadata (`r2_key`) no Mongo. Novas fotos deixam de pesar no banco. Os leitores (já migrados na Fase 1) as encontram.
3. **Fase 3 — Migração dos dados antigos + limpeza:** script que lê os docs com `data_b64`, sobe pro R2, grava `r2_key`, remove `data_b64` — libera o espaço já ocupado.

---

## File Structure

- **Create** `backend/services/image_store.py` — núcleo: `salvar_imagem`, `carregar_bytes`, `bytes_do_doc`, `r2_ativo`, `_EXT_POR_TIPO`.
- **Create** `backend/tests/test_image_store.py` — testes do núcleo (R2 mockado + fallback base64).
- **Create** `backend/scripts/migrar_imagens_para_r2.py` — Fase 3 (migração dos data_b64 existentes).
- **Modify** `backend/routes/uploads.py` — escrita (Fase 2) e leitura (`get_image`, `get_image_tiff`, `image_metadata`).
- **Modify (leitura → image_store):** `backend/routes/ptam.py`, `backend/routes/assinatura.py`, `backend/routes/contratos.py`, `backend/routes/contratos_exclusividade.py`, `backend/routes/locacao.py`, `backend/routes/propostas.py`, `backend/routes/tvi.py`, `backend/services/recibo_anexos.py`, `backend/routes/galeria.py`, `backend/server.py`.
- **Não mexer (fora de escopo):** assinaturas manuscritas em `models/tvi.py`/`tvi_pdf.py`/`tvi_docx.py` (o `data_b64` ali é o traço da assinatura embutido no próprio documento da vistoria — pequeno, não é imagem de upload). `routes/zayra.py` e `pdf_converter` são cobertos indiretamente via `image_store`.

---

## Convenção da key no R2

`images/{user_id}/{image_id}.{ext}` — `ext` derivada do content_type (`jpg`/`png`/`webp`/`pdf`). Prefixo `images/` distinto de `assinatura-cliente/`, `zapi-tmp/`, `branding/` etc. (não colide com o bucket de PDFs de assinatura — ver memória de infra sobre lifecycle do bucket).

---

### Task 1: Núcleo `image_store`

**Files:**
- Create: `backend/services/image_store.py`
- Test: `backend/tests/test_image_store.py`

- [ ] **Step 1: Escrever o módulo**

```python
# @module services.image_store — armazenamento único das imagens/fotos de upload.
# Prioriza o Cloudflare R2 (só metadata leve no Mongo); cai para base64-no-Mongo
# quando o R2 não está configurado ou falha. A LEITURA resolve os dois formatos
# (r2_key novo OU data_b64 legado), então imagens antigas continuam abrindo.
import asyncio
import base64
import logging
import os
import uuid
from datetime import datetime

from services import r2_storage

logger = logging.getLogger("romatec")

_EXT_POR_TIPO = {
    "image/jpeg": "jpg", "image/jpg": "jpg", "image/png": "png",
    "image/webp": "webp", "application/pdf": "pdf",
}


def r2_ativo() -> bool:
    return bool(os.getenv("R2_ENDPOINT") and os.getenv("R2_BUCKET")
                and os.getenv("R2_ACCESS_KEY") and os.getenv("R2_SECRET_KEY"))


async def salvar_imagem(db, uid, data, content_type, filename, image_id=None, extra=None):
    """Persiste uma imagem e devolve o doc gravado (com 'id'). Sobe no R2 quando
    disponível (grava só 'r2_key'); senão grava 'data_b64' (comportamento legado)."""
    image_id = image_id or str(uuid.uuid4())
    doc = {
        "id": image_id, "user_id": uid, "filename": filename,
        "content_type": content_type, "size_bytes": len(data),
        "created_at": datetime.utcnow(),
    }
    if extra:
        doc.update(extra)
    if r2_ativo():
        ext = _EXT_POR_TIPO.get((content_type or "").lower(), "bin")
        key = f"images/{uid}/{image_id}.{ext}"
        try:
            await asyncio.to_thread(r2_storage.upload_bytes, data, key, content_type)
            doc["r2_key"] = key
        except Exception:
            logger.exception("image_store: R2 falhou; gravando base64 no Mongo (fallback) id=%s", image_id)
            doc["data_b64"] = base64.b64encode(data).decode("utf-8")
    else:
        doc["data_b64"] = base64.b64encode(data).decode("utf-8")
    await db.images.insert_one(doc)
    return doc


async def bytes_do_doc(doc):
    """Bytes a partir de um doc de db.images já carregado (R2 OU base64). None se ausente."""
    if not doc:
        return None
    b64 = doc.get("data_b64")
    if b64:
        try:
            return base64.b64decode(b64)
        except Exception:
            logger.warning("image_store: data_b64 corrompido id=%s", doc.get("id"))
            return None
    key = doc.get("r2_key")
    if key:
        try:
            return await asyncio.to_thread(r2_storage.download_bytes, key)
        except Exception:
            logger.exception("image_store: falha ao baixar do R2 key=%s", key)
            return None
    return None


async def carregar_bytes(db, image_id, uid=None):
    """Busca o doc por id (opcionalmente escopado por user_id) e resolve os bytes."""
    if not image_id:
        return None
    filtro = {"id": image_id}
    if uid is not None:
        filtro["user_id"] = uid
    doc = await db.images.find_one(filtro)
    if not doc and uid is not None:
        doc = await db.images.find_one({"id": image_id})
    return await bytes_do_doc(doc)


async def remover_imagem(db, image_id, uid=None):
    """Remove a imagem do R2 (se houver) e o doc do Mongo."""
    filtro = {"id": image_id}
    if uid is not None:
        filtro["user_id"] = uid
    doc = await db.images.find_one(filtro)
    if not doc:
        return False
    key = doc.get("r2_key")
    if key:
        try:
            await asyncio.to_thread(r2_storage.delete_object, key)
        except Exception:
            logger.warning("image_store: falha ao remover do R2 key=%s (segue removendo do Mongo)", key)
    await db.images.delete_one({"id": image_id})
    return True
```

- [ ] **Step 2: Escrever os testes** (`backend/tests/test_image_store.py`)

```python
import base64
import pytest
from services import image_store


class FakeImages:
    def __init__(self):
        self.docs = {}
    async def insert_one(self, doc):
        self.docs[doc["id"]] = doc
    async def find_one(self, filtro):
        for d in self.docs.values():
            if all(d.get(k) == v for k, v in filtro.items()):
                return d
        return None
    async def delete_one(self, filtro):
        self.docs = {k: v for k, v in self.docs.items() if v.get("id") != filtro.get("id")}


class FakeDB:
    def __init__(self):
        self.images = FakeImages()


@pytest.mark.asyncio
async def test_salva_base64_quando_sem_r2(monkeypatch):
    monkeypatch.setattr(image_store, "r2_ativo", lambda: False)
    db = FakeDB()
    doc = await image_store.salvar_imagem(db, "u1", b"\xff\xd8\xffdata", "image/jpeg", "f.jpg")
    assert doc.get("data_b64") and "r2_key" not in doc
    assert await image_store.carregar_bytes(db, doc["id"]) == b"\xff\xd8\xffdata"


@pytest.mark.asyncio
async def test_salva_no_r2_quando_ativo(monkeypatch):
    monkeypatch.setattr(image_store, "r2_ativo", lambda: True)
    subiu = {}
    def fake_upload(data, key, content_type, **kw):
        subiu["key"], subiu["data"] = key, data
        return "http://r2/" + key
    monkeypatch.setattr(image_store.r2_storage, "upload_bytes", fake_upload)
    monkeypatch.setattr(image_store.r2_storage, "download_bytes", lambda k: subiu["data"])
    db = FakeDB()
    doc = await image_store.salvar_imagem(db, "u1", b"PNGdata", "image/png", "f.png")
    assert doc.get("r2_key", "").startswith("images/u1/") and "data_b64" not in doc
    assert await image_store.carregar_bytes(db, doc["id"]) == b"PNGdata"


@pytest.mark.asyncio
async def test_carrega_legado_data_b64(monkeypatch):
    monkeypatch.setattr(image_store, "r2_ativo", lambda: True)
    db = FakeDB()
    await db.images.insert_one({"id": "old1", "user_id": "u1",
                                "data_b64": base64.b64encode(b"antigo").decode()})
    assert await image_store.carregar_bytes(db, "old1") == b"antigo"


@pytest.mark.asyncio
async def test_r2_falha_cai_para_base64(monkeypatch):
    monkeypatch.setattr(image_store, "r2_ativo", lambda: True)
    def boom(*a, **k):
        raise RuntimeError("r2 down")
    monkeypatch.setattr(image_store.r2_storage, "upload_bytes", boom)
    db = FakeDB()
    doc = await image_store.salvar_imagem(db, "u1", b"xy", "image/png", "f.png")
    assert doc.get("data_b64") and "r2_key" not in doc
```

- [ ] **Step 3: Rodar** `py -m pytest tests/test_image_store.py -v` → todos PASS.
- [ ] **Step 4: Commit** `git commit -m "feat(images): núcleo image_store (R2 + fallback base64)"`

---

### Task 2 (FASE 1): Leitores → `image_store` (por arquivo)

Para CADA arquivo abaixo, o padrão de substituição é:

```python
# ANTES:
img = await db.images.find_one({"id": iid})            # ou com projeção {"data_b64": 1}
if img and img.get("data_b64"):
    raw = base64.b64decode(img["data_b64"])

# DEPOIS:
from services import image_store            # no topo do arquivo
raw = await image_store.carregar_bytes(db, iid)
if raw:
    ...
```

Regras:
- Onde havia projeção `{"data_b64": 1}`, remover a projeção (ou incluir `"r2_key": 1`) — `carregar_bytes` faz a busca completa.
- Onde já se tem o `doc` em mãos (loop que reusa o find_one), usar `await image_store.bytes_do_doc(doc)` e garantir que o `find_one` NÃO exclua `r2_key`.
- Manter transformações existentes (`downscale_image(...)`, `data:{ct};base64,...`) — só troca a ORIGEM dos bytes.

- [ ] **Step A:** `routes/ptam.py` (todos os `_preload_*`; ~40 sites, linhas listadas no grep). Rodar `py -m pytest tests/ -k ptam -q` após.
- [ ] **Step B:** `routes/assinatura.py` (linhas 129-541). Rodar suíte de assinatura.
- [ ] **Step C:** `routes/contratos.py` (`_carregar_anexos_bytes`, 1072-1076) + `contratos_exclusividade.py` (136-137).
- [ ] **Step D:** `routes/locacao.py` (124-247) + `routes/propostas.py` (123-134).
- [ ] **Step E:** `routes/tvi.py` (375-379: fotos por página; NÃO tocar `data.data_b64` de assinatura) + `services/recibo_anexos.py` (132-135).
- [ ] **Step F:** `routes/galeria.py` (48/111/159 — usa db.images? confirmar coleção própria; se `db.images`, migrar leitura) + `server.py` (230-232, logo do folder).
- [ ] **Step commit por arquivo:** `git commit -m "refactor(images): <arquivo> lê via image_store"`

**Verificação da Fase 1:** build/import de todos os routers (`py -c "import routes"`), suíte pytest verde. Comportamento idêntico (ainda lê data_b64; R2 ainda não ativo na escrita).

---

### Task 3 (FASE 2): Escrita no R2 — `routes/uploads.py`

**Files:** Modify `backend/routes/uploads.py`

- [ ] **Step 1:** `upload_image` — imagem direta: trocar o bloco que monta `doc` + `insert_one` por:

```python
from services import image_store
doc = await image_store.salvar_imagem(db, uid, data, detected_content_type, nome)
image_id = doc["id"]
```

- [ ] **Step 2:** `upload_image` — PDF: o original de auditoria e cada página PNG passam a usar `image_store.salvar_imagem(db, uid, png, "image/png", f"{base_nome} (p. {idx}/{total}).png", extra={"convertido_de_pdf": True, "source_pdf_id": original_id, "pdf_page": idx, ...})`. O `original_id` = `salvar_imagem(..., content_type="application/pdf", extra={"is_original_pdf": True, ...})["id"]`.
- [ ] **Step 3:** `get_image`, `get_image_tiff`, `image_metadata` — trocar `base64.b64decode(doc["data_b64"])` por `await image_store.bytes_do_doc(doc)` (o `get_image` continua PROXY: baixa e serve; a URL `/api/upload/image/{id}` não muda → frontend intacto). `delete_image` → `image_store.remover_imagem`.
- [ ] **Step 4:** Verificar que `get_image` sem `data_b64` e sem `r2_key` retorna 404 (guard existente).
- [ ] **Step 5:** Commit `git commit -m "feat(images): upload grava no R2 (fallback Mongo)"`

**Verificação da Fase 2 (produção):** com `R2_*` setadas no Railway, subir uma foto nova → conferir no R2 que o objeto `images/{uid}/...` existe, a foto abre na tela E entra no PDF do laudo gerado. Sem `R2_*`, comportamento cai no base64 (idêntico ao atual).

---

### Task 4 (FASE 3): Migração dos data_b64 existentes → R2

**Files:** Create `backend/scripts/migrar_imagens_para_r2.py`

- [ ] **Step 1:** Script que itera `db.images.find({"data_b64": {"$exists": True}})` em lotes; para cada: sobe os bytes no R2 (`images/{user_id}/{id}.{ext}`), `update_one` setando `r2_key` e `$unset data_b64`. Idempotente (pula quem já tem `r2_key`). Log de progresso + total de bytes liberados. `--dry-run` por padrão; `--apply` para efetivar.
- [ ] **Step 2:** Rodar `--dry-run` (conta quantos/quanto). Depois `--apply` (roda contra o Railway Mongo já migrado). Só então o espaço é liberado.
- [ ] **Step 3:** Commit `git commit -m "chore(images): script de migração data_b64 -> R2"`

---

## Self-Review

- **Cobertura:** upload (Task 3), leitura na tela (get_image, Task 3), leitura nos PDFs/DOCX (Task 2, todos os preloaders), dados antigos (Task 4). ✓
- **Sem regressão na Fase 1:** `carregar_bytes` lê `data_b64` idêntico ao atual quando não há R2. ✓
- **Nomes consistentes:** `salvar_imagem`/`carregar_bytes`/`bytes_do_doc`/`remover_imagem`/`r2_ativo` usados igual em todas as tasks. ✓
- **Fora de escopo confirmado:** assinaturas manuscritas da TVI (traço embutido, não upload). ✓
- **Versionamento:** ao concluir, bump `frontend/build-number.txt` + `CACHEBUST_BACKEND` (regra do CLAUDE.md).
