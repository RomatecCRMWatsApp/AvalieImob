[build]
builder = "nixpacks"
buildCommand = "npm install --legacy-peer-deps && cd packages/frontend && npm run build && cd ../backend && npm run bundle"

[deploy]
startCommand = "node packages/backend/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyMaxRetries = 3
